import { AppShell } from "@famax/portal"; import { headers } from "next/headers"; import { redirect } from "next/navigation"; import { assertAuthConfig, auth } from "@famax/auth";
export default async function Workspace({ children }: Readonly<{ children: React.ReactNode }>) { let user = { name: "Demo workspace", mesRole: "ADMIN" }; if (process.env.NODE_ENV === "production" || process.env.DATABASE_URL) { const requestHeaders = await headers(); assertAuthConfig(); const session = await auth.api.getSession({ headers: requestHeaders }); if (!session) redirect("/login"); user = { name: session.user.name, mesRole: (session.user as typeof session.user & { mesRole?: string }).mesRole ?? "OPERATOR" }; } return <AppShell user={user}>{children}</AppShell>; }
